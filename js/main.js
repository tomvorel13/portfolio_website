$(document).ready(function(){
  $('.items').animsition({
    inClass: 'fade-in-left',
    outClass: 'fade-out-right',
    inDuration: 1200,
    loading: false,
  });

  $('.main').animsition({
  inClass: 'fade-in-left',
  outClass: 'fade-out-right',
  inDuration: 1200,
  loading: false,
});

});
